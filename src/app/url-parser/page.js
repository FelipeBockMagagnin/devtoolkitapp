import UrlParser from "@/components/url-parser";

export const metadata = {
    title: 'URL Parser - Dev Toolkit',
    description: 'Parse a URL into its individual components!',
}

export default function Page() {
    return <UrlParser/>
}