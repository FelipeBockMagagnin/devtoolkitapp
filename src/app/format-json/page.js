import FormatJson from "@/components/format-json";

export const metadata = {
    title: 'Json Formatter - Dev Toolkit',
    description: 'Format and beautify your json in one click, maximize website performance with this efficient JSON formatter online free tool.',
}

export default function Page() {
    return <FormatJson/>
}